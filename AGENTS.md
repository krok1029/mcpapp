## Agent skills

### Issue tracker

Issue 以本機 Markdown 檔案記錄於 `.scratch/`。詳細規則請參閱 `docs/agents/issue-tracker.md`。

### Triage labels

本專案採用五個預設的分流標籤。詳細定義請參閱 `docs/agents/triage-labels.md`。

### Domain docs

本專案採用單一脈絡（single-context）的領域文件配置。詳細規則請參閱 `docs/agents/domain.md`。

### Development workflow

本專案從訪談、Spec、拆票、實作、Review 到驗收，必須依序套用指定 skills。詳細流程與 skill 規則衝突的優先序請參閱 `docs/agents/development-workflow.md`。
