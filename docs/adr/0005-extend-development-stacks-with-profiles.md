# 以開發棧設定檔擴充語言與框架

核心工作流、核准、證據鏈與知識圖譜不直接依賴特定語言或框架，而由開發棧設定檔提供專案建立、套件管理、Build、Test、Lint 與程式碼分析能力。MVP 只實作 TypeScript 設定檔，並要求所有 TypeScript 框架提供名稱固定的 `typecheck`、`lint`、`test` 與 `build` 指令；不適用時必須明確宣告理由，不能以空指令假裝通過。這接受少量額外抽象成本，以便後續增加其他語言與框架而不改寫工作流核心。
